{{album.artist.name}}<br>
{{album.name}}<br>
{{album.year}}<br>
{{album.totalSold}}<br>
{{album.updatedAt}}<br>
{{album.updatedAtShort}}<br>

{{audio-player url="/song.ogg"}}

{{#each album.comments as |comment|}}
  {{comment.message}}
{{else}}
  No comments found.
{{/each}}

{{outlet}}
